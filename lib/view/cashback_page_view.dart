import 'package:flutter/material.dart';

class CashbackPageView extends StatelessWidget {
  const CashbackPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Cashback"),
      ),
    );
  }
}
